[project]
name = "new-spider-xhs-test002"
version = "0.1.0"
description = "Spider_XHS 采集工具（src 布局：源码在 src/spider_xhs，入口 main.py，供 uv sync / uv run）"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "PyExecJS>=1.5.1",
    "requests>=2.31.0",
    "loguru>=0.7.0",
    "python-dotenv>=1.0.0",
    "retry>=0.9.2",
    "openpyxl>=3.1.0",
    "pillow>=10.0.0",
    "qrcode>=7.4.0",
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
]
